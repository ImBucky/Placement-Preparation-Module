class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        from collections import Counter
        res1=Counter(s)
        res2=Counter(t)
        if res1==res2:
            return True
        else:
            return False
        
